{
  "name": "remove-background",
  "version": "1.0.0",
  "description": "This is a simple web-based tool for removing the background from images.",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
